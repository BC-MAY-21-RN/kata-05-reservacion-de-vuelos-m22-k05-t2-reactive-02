import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontSize: 32,
    fontWeight: 'bold',
    color: 'black',
    marginHorizontal: 30,
  },
});

export default styles;
